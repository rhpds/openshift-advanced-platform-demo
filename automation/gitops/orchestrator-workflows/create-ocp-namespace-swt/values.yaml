cluster:
  subdomain: ""

persistence:
  postgresql:
    secretRef:
      name: backstage-psql-secret-developer-hub
      userKey: POSTGRES_USER
      passwordKey: POSTGRES_PASSWORD
    serviceRef:
      name: backstage-psql-developer-hub
      port: 5432
      databaseName: backstage_plugin_orchestrator
      databaseSchema: create-ocp-namespace-swt

image:
  repository: ghcr.io/redhat-ads-tech/create-ocp-namespace-swt
  tag: 0.1.11

env:
  swtRef: "template:default/orchestrator-namespace-request"

vault:
  secretStore: vault-secret-store
