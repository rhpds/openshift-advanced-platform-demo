apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  labels:
    {{- include "ztunnel-healer.labels" . | nindent 4 }}
  # Must NOT have istio.io/dataplane-mode: ambient — the healer
  # probes HBONE port 15008 directly and must not have its own
  # traffic intercepted by ZTunnel.
